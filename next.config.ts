import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Everything on this site is statically generated — no server features used.
  reactStrictMode: true,
};

export default nextConfig;
